import { SideBarUserDetails } from './SideBarUserDetails'

const EmptySideBar = () => {
    return (
        <div className="sidebar">
            <SideBarUserDetails/>
        </div>
    );
}

export default EmptySideBar;